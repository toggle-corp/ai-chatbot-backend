{{- if .Values.streamlit.ingress.enabled }}

apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ template "tc-chatbot-backend.fullname" . }}-streamlit-ingress
  labels:
    app: {{ template "tc-chatbot-backend.name" . }}
    component: streamlit-service
    environment: {{ .Values.app.environment }}
    release: {{ .Release.Name }}
spec:
  ingressClassName: {{ .Values.streamlit.ingress.className }}
  rules:
    - host: {{ required "streamlit.ingress.host" .Values.streamlit.ingress.host | quote }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ template "tc-chatbot-backend.fullname" . }}-streamlit-svc
                port:
                  number: 80

{{- end }}
