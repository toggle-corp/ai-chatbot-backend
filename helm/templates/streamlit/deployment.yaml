{{- if .Values.app.streamlit.enabled }}

apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ template "tc-chatbot-backend.fullname" . }}-streamlit
  labels:
    component: streamlit-deployment
    environment: {{ .Values.app.environment }}
    release: {{ .Release.Name }}
    # XXX: Add global lables?
spec:
  replicas: {{ .Values.app.streamlit.replicaCount }}
  selector:
    matchLabels:
      app: {{ template "tc-chatbot-backend.name" . }}
      release: {{ .Release.Name }}
      run: {{ .Release.Name }}-streamlit
  template:
    metadata:
      labels:
        app: {{ template "tc-chatbot-backend.name" . }}
        release: {{ .Release.Name }}
        run: {{ .Release.Name }}-streamlit
    spec:
      containers:
        - name: {{ .Chart.Name }}-streamlit
          image: "{{ .Values.app.streamlit.image.name }}:{{ .Values.app.streamlit.image.tag }}"
          imagePullPolicy: {{ .Values.app.image.pullPolicy }}
          command: ["streamlit", "run", "app.py", "--server.address", "0.0.0.0"]
          ports:
            - name: http
              containerPort: {{ .Values.app.streamlit.containerPort }}
              protocol: TCP
          livenessProbe: # FIXME: Fix Liveness Probe
            httpGet:
              path: /  # TODO
              port: {{ .Values.app.streamlit.containerPort }}
            initialDelaySeconds: 10180  # TODO:?
            periodSeconds: 5  # TODO:?
          resources:
            requests:
              cpu: {{ .Values.app.streamlit.resources.requests.cpu }}
              memory: {{ .Values.app.streamlit.resources.requests.memory }}
            limits:
              cpu: {{ .Values.app.streamlit.resources.limits.cpu }}
              memory: {{ .Values.app.streamlit.resources.limits.memory }}
          env:
            - name: SERVER_IP
              value: {{ template "tc-chatbot-backend.fullname" . }}-api-svc
            - name: SERVER_PORT
              value: {{ .Values.app.api.containerPort | quote }}

{{- end }}
